MY MANAGER - PLANNING TRACKER (2026-09-09 session)
===================================================
Source: the owner's live UI audit (pasted directive, 2026-09-09).
Legend: [ ] OPEN  [~] IN PROGRESS  [x] DONE  [?] NEEDS OWNER DECISION
Owner rule: tasks run ONE BY ONE (no parallel work). After every phase,
update this file + CONTINUATION-DIRECTIVE.md STATUS LOG + reflection.txt.

--------------------------------------------------------------------
WAVE 2026-09-16 - SESSION RESUME: app.html inline-block mystery SOLVED
--------------------------------------------------------------------
X1  [x] Root cause of "inline script block 4 never evaluates on
        app.html" (mystery carried over from the 2026-09-15 session
        that crashed mid-diagnosis): the QA server on :8765 was a
        STALE serve.cjs PROCESS started BEFORE the inline script was
        edited. It served fresh HTML (new inline code) with the OLD
        in-memory CSP hash list, so Chrome silently blocked block 4
        (the 59,915-char dashboard block: DASH_ACTION_MAP,
        activeProject, damKind, demo rail toggle, #dam modal).
        No console error surfaced via the harness (CSP block). Proof:
        live header carried hul1mRK5/u1Ky6uEU9 (pre-edit hashes);
        AuMH9SHj2lGD (new block 4) was ABSENT from the live header
        while present on disk in worker.js + serve.cjs.
X2  [x] Fix: verified PID by command line (wmic/Get-CimInstance:
        node.exe serve.cjs, PID 16668), taskkill //PID only (never by
        image name), restarted serve.cjs. Live CSP now carries the
        new hash; page verified in agent-browser: activeProject/
        damKind defined, grid + 3 demo cards render, zero page
        errors.
X3  [x] Feature verification of the 2026-09-15 second-pass wave
        (previously blocked by the dead inline block): demo rail row
        toggles mmgr_demos_hidden 1<->0, section hides/shows, row
        relabels Show/Hide demo projects, aria-pressed syncs; #dam
        modal opens with Delete disabled, typing "confirm"
        auto-normalizes to CONFIRM and enables the button, cancel
        closes. All clean.
X4  [x] Gates re-run after fix: npm run verify 23 PASS/exit 0;
        qa-dashboard-spec 87/87 (incl. per-hash worker+serve
        checks); qa-market-features 61/61.
X5  [x] LESSON written into AGENTS.md hard-won knowledge #10 (owner
        directive 2026-09-16): after editing an inline <script> in any
        served page, a RUNNING serve.cjs keeps serving the stale CSP
        until restarted - restart it or expect "page looks intact but
        its JS is dead". Diagnostic shortcut: diff the sha256 of each
        inline block on disk against the hashes in the LIVE response
        header, not just against worker.js/serve.cjs on disk.

--------------------------------------------------------------------
AUDIT 2026-09-16 (POST-DEPLOY) - banner stack + nav drawer, real viewports
--------------------------------------------------------------------
A1  [x] Deployed-bytes check: prod /project HTML has banners inside
        <header>, /dist/mmgr.min.css byte-identical to staging (guard
        + banner rules present). First probe used the WRONG URL
        (/css/... -> 404 fallback page) - re-verified against /dist
        before believing anything (skeptical-audit rule).
A2  [x] Live walk (agent-browser, prod): desktop 1262px light+dark -
        banner 28px static in header, header 0-112, --hdr-h 112,
        sidebar top 112, SECTIONS title on screen, nav-close-row
        display:none even with body.nav-open forced. Boundary:
        769px none / 768px flex-with-nav-open (exact media flip).
        Mobile 390px: burger opens drawer, X visible+clickable
        (hit-tested), closes drawer, banner stack intact, light+dark.
        app.html both viewports clean. Zero page errors throughout.
A3  [x] DEFECT FOUND + FIXED (two passes): --hdr-h early-measurement
        race. Boot measured #app-header before the webfont swap
        settled; mobile light held 216px vs real 201px and the fixed
        sidebar/#ai-win sat 15px low until a coincidental resize.
        Pass 1 (895444c, SW v289): re-measure on load + fonts.ready -
        STILL LOST in prod (fonts can resolve before the LATE render
        that first requests the webfont; event-order guessing).
        Pass 2 (e8a0756, SW v290): ResizeObserver on #app-header -
        by-construction tracking of ANY height change; no feedback
        loop (consumers never resize the header). Verified: header
        perturbed 159->239->159 with --hdr-h tracking exactly; 15
        stability samples = 1 unique value (no oscillation); final
        prod check mobile light 201==201, demo 30 tasks, zero errors.
        NOTE: v289 shipped the fonts-only bundle -> v290 bump was
        REQUIRED or those clients would cache the interim bundle.
A4  [x] FALSE ALARMS caught by re-verification (not shipped as bugs):
        mobile drawer "booting open" was a desktop->mobile viewport
        resize artifact (real phones load at their width; fresh
        390px load clean); "empty demo dashboard" was my test
        clearing localStorage AFTER the launcher boot that seeds
        mmgr_state_demo-filled (real flow: launcher boot seeds, then
        Explore opens 30-task project); offsetParent-null for
        position:fixed elements is spec, not invisibility (hit-test
        proved clickable).
A5  [x] Deploys: 4a24a2e3 (v289 fonts fix), ece23a7f (v290 RO fix);
        CI green on 895444c (run 35114583196) and e8a0756 (run
        35118481948). Bookkeeping commit docs-only, no redeploy
        needed (no served-file delta).

--------------------------------------------------------------------
SHIP 2026-09-16 (2) - commit 30c92e9, CI run 35135676302 GREEN, deploy
d40a8f43 - MCP-ON-API-KEYS + FREE-TEXT-AI wave (owner directive doc
PROJECT-API-KEY-AUDIT-AND-DIRECTIVE-2026-09-16.md, owner-approved in chat):
M1  [x] MCP unified onto cloud_api_keys: Bearer sk-mmgr-/X-API-Key auth,
        scope-projected get_* tools, apply_changes QUEUES a pending mcp
        review (the old path called changelog/import, which never wrote
        state), mcp accept branch now applies to R2 (was log-only), MCP
        route rate-limited (audit F1), timing-sink gap closed (audit F5).
M2  [x] AI: honest 503 for Workers-AI capacity (was misleading 401
        'missing api key'); local tier free-text rides the relay with
        project context instead of refusing; project-only topic gate on
        both tiers (chit-chat answered locally, zero model calls).
M3  [x] Docs: guide A-27 'Connect an Outside AI' + nav/footer +
        features/contact counts 25->28; share.js key banner + API Keys
        panel fine print with the MCP address + guide link. Guide grid
        min-width:0 + code overflow-wrap fix (mkt-17 390px overflow,
        pre-existing from v288 key samples).
M4  [x] QA: qa-api-keys +P12-P17 (MCP E2E: init, scoped refusal, granted
        read, queue->accept->applied, owner parity) 30/30; qa-ai +A08t
        (topic gate) +A08u (relay assist, caught a real window.prompt
        scope bug pre-ship); qa-ai-relay R01 -> 503 contract; verify +
        T1 6/6 + rank9 31/31 + cloud-phase2 85/85 (after killing a
        serve.cjs port collision - NOT an app failure) + marketing 20/20.
M5  [x] Prod smoke: MCP GET 405 JSON, bad bearer generic 403, ai/chat
        503 capacity message, guide A-27 live via /mymanager-field-guide,
        SW v291, dist/bundle.js carries the MCP address. NOTE: the test
        key sk-mmgr-TKN65Y-...RKH8GR was pasted into a Claude chat 3x
        during the owner's live test - owner must revoke it (API Keys
        panel) and mint a fresh one.
--------------------------------------------------------------------
SHIP 2026-09-16 (3) - commit 2f8acba, CI run 35139037123-era GREEN,
deploy ec4ad424 - MCP PATH-A (owner decision in chat, fix plan reviewed):
P1  [x] Root cause: Claude Desktop's connector probes MCP endpoints
        WITHOUT credentials; a bare HTTP 401 makes spec clients treat the
        server as an OAuth-protected resource and discovery fails against
        routes that do not exist. Path A chosen over minimal OAuth:
        transport answers open, auth lives at the tool layer.
P2  [x] src/mcp/server.js: initialize/notifications/tools/list answer
        unauthenticated; every tools/call gated by auth-null check ->
        normal MCP isError refusal (MCP_AUTH_REFUSAL export), no data.
        Transport-level 401/403 removed entirely; cloudForbidden import
        dropped. Bad/unknown Bearer = same per-tool refusal (no oracle).
P3  [x] serve.cjs mirror carries the same contract (byte-identical
        refusal text, CommonJS copy since it cannot import the ESM module).
P4  [x] Gates reworded + added: qa-ai-relay M1 (open handshake) + M1b
        (tool-gate refusal, no-DB proof), qa-api-keys P17b (bad Bearer ->
        200 isError) + P17c (anon init -> serverInfo), qa-full mcp3
        reworded + mcp7 added (181/181). Battery: verify, T1 6/6,
        ai-relay, api-keys E2E ALL GREEN.
P5  [x] Docs: share.js fine print + guide A-27 gain the 'No sign-in'
        instruction (leave connector sign-in off; key checked only when a
        tool asks). SW v291 -> v292 (share.js bundle bytes changed).
P6  [x] Prod smoke: GET 405 unchanged; unauth initialize -> 200
        serverInfo; unauth tools/call -> 200 isError refusal; guide
        'No sign-in' live; SW v292 live. LESSON: on an MCP endpoint the
        HTTP status IS API - return auth errors as MCP isError results,
        never as transport-level 401/403.
--------------------------------------------------------------------
SHIP 2026-09-16 - commit 17af8a2, CI run 35108594436 GREEN, deploy 2a5f770e
--------------------------------------------------------------------
S1  [x] Full local CI before push: npm run verify 23 PASS; T1 6/6
        (qa-dashboard-spec 87, qa-changelog-diffs, qa-ai-relay,
        verify-report-issue 27, verify-dynamic-labels,
        verify-delegate-gate); wrangler dry-run; T2 verify suites
        (verify-cloud-autosave-signin 8/0, verify-controls-admin 11/0,
        qa-api-keys all gates).
S2  [x] Human-audit walk (skeptical-code-audit + agent-browser):
        app.html both themes, demo toggle + #dam modal exercised,
        marketing pages + admin + dashboard console sweep - 11 pages
        zero page errors.
S3  [x] Two audit fixes shipped in 17af8a2:
        (a) .nav-close-row desktop guard - its styling was mobile-only
        (inside @media max-width:768px) while the markup was
        unconditional, so an unstyled stray X rendered at the top of
        #app-main on desktop; fix = base .nav-close-row{display:none}
        in the desktop Section Navigation section (mobile
        body.nav-open rule outranks it).
        (b) Banner stack - the four chrome banners (safety, readonly,
        editor-scope, client-scope) were sticky elements in body flow
        ABOVE the sticky header; on read-only/demo projects the
        header sat 28px down while the fixed sidebar top was computed
        from --hdr-h (header height only), clipping the SECTIONS
        title. Fix = banners moved INSIDE #app-header as full-width
        rows (header flex-wrap:wrap, banners position:static via
        #app-header > [id$="-banner"]), so --hdr-h is always the true
        chrome height. Verified live: sidebar top == header bottom ==
        var value on a fresh profile; mobile drawer close still works
        at 390px.
S4  [x] Push -> poll Actions (CI repaired-loop rules followed: poll
        status/completed, run is the truth) -> GREEN -> tar staging
        recipe under $HOME/mmgr-deploy with staged-copy verification
        (guard CSS, banner-in-header, new hashes all confirmed in the
        staged tree BEFORE deploy) -> wrangler deploy 2a5f770e ->
        prod smoke: 307 /app.html->/app, CSP carries AuMH9SHj
        (current block 4), prod project HTML has banner inside
        header, minified CSS carries the nav-close guard, emoji scan
        clean, /app inline block evaluates (damKind live), demo
        toggle flips 1<->0 in prod, zero page errors.

--------------------------------------------------------------------
WAVE 2026-09-15 - OWNER DIRECTIVE (dots, ring, brand, guide, launcher)
--------------------------------------------------------------------
W1  [x] Mobile scroll shrink (app page): prior text-size-adjust fix is
        IN the served CSS; owner reproduction traced to Android
        high-DPI URL-bar viewport resize (iPhone shows none). Root
        answer: Android keyboard/url-bar resize is OS-driven; app uses
        dvh + text-size-adjust already. Monitoring item, no code defect.
W2  [x] Performance mode toggle audited: js/mmgr-perf.js adds
        body.perf-mode which disables the starfield/glass animation
        layers (css [data-perf] blocks) - documented in directive.
W3  [x] Dot mechanism: Attention card (red dot + text, no circles).
W4  [x] Dot mechanism: This Week Plan/Completed/Carryover dot + number,
        strict red/green/amber palette only.
W5  [x] Dot mechanism: Project Health rows dot + number.
W6  [x] Project Completion % ring polished (gradient, glow, animation).
W7  [x] Brand icon replaced by original MPW monogram (css/mpw-mark.svg,
        sprite i-mpw) on marketing pages + launcher rail; Gemini sparkle
        path removed everywhere served.
W8  [x] Field guide: em-dashes verified 0; roadmap collapsibles (steps +
        what-completed-looks-like) after key task explanations.
W9  [x] Index feature cards: borders toned down + small SVG watermarks,
        text stays readable.
W10 [x] AI auto-enabled by default in project (default tier 'local',
        zero-key); turning it off still available.
W11 [x] Mobile hamburger drawer cutoff (project page): drawer now top
        chrome (z 960), full height, own close button (.nav-close-row).
W12 [x] app.html: Demo projects section hide via eye icon (persists,
        demos stay in the rail Demos group; rail click reveals section).
W13 [x] Account deletion section in app settings rail (danger link +
        type-DELETE confirm + POST /api/auth/delete-account removes
        account and all projects; email accounts only).
W14 [~] Project API keys - SUB-TASK PLAN (execute one sub-task at a
        time; do NOT bulk):
        P1 [x] migrations/0020_cloud_api_keys.sql (project_id, label,
             scope JSON per CLOUD_SECTIONS, expires_at, key_salt,
             key_hash PBKDF2, key_fingerprint lookup, key_prefix,
             active, created_at, last_used_at).
        P2 [x] src/cloud/api-keys.js: create/list/revoke handlers;
             owner-session or owner-code auth; active-key cap (10);
             plaintext returned ONCE at create.
        P3 [x] src/router.js routes: POST/GET/DELETE
             /api/cloud/projects/:id/api-keys (rate-limited bucket).
        P4 [x] cloudAuthApiKey(request, env, projectId): X-API-Key
             fingerprint lookup, hash verify, expiry + project match,
             generic 403 on every miss, last_used_at touch.
        P5 [x] Scoped-save enforcement for API keys: reuses the editor
             section merge (cloudScopeMerge); every API-key write lands
             in the review queue (source_type 'api'); scoped load
             projects only granted sections. Owner accepts/rejects from
             inside the project with field-level diffs.
        P6 [x] Project Controls UI: API keys card in Share & Access
             (label + section checkboxes + expiry picker + list with
             last-used + revoke), shown-once banner reuse, action map
             entries in mmgr-app.js.
        P7 [x] Field guide blurb: API KEY card in the cloud-sharing
             sheet (what keys are, scoping, expiry, review queue).
        P8 [x] tools/qa-api-keys.cjs T2 harness (26 gates, ALL GREEN
             locally) + ci.yml T2 step on :8809 + summary row.
        P9 [x] Bookkeeping (this file + directive + reflection) + ship.
        P10 [x] LIVE VERIFICATION (owner asked for the curl walkthrough):
             wrangler dev :8812, 17 curl probes green - mint, scoped
             read (wbs->tasks, res->resources, bud excluded), scoped
             save with out-of-scope budget BLOCKED (applied/blocked +
             field diffs), owner state unchanged, review row sourceType
             'api' + label, accept applies, revoke -> generic 403,
             second key still scoped. BUG FOUND+FIXED: create ignored
             expiresInDays (now accepted as an alias of expiresAt);
             qa-api-keys re-run ALL GREEN.
W14 [x] DONE - shipped end to end.
W15 [~] Final gate: node build.js DONE, npm run verify GREEN (exit 0,
        SW bump mmgr-shell-v288), emoji scan 0, T1 harnesses green
        (dashboard-spec 87/87, market-features 61/61, ai-polish 11/11,
        ai-relay PASS, verify-* all PASS); qa-rhythm NOW 11/11 PASS
        (W15's earlier 'pre-existing' note was WRONG: the baseline
        worktree check was invalid - server served the working tree.
        R01/R10 were this wave's nav-close-row edit replacing the
        panel-dash opening tag - FIXED; R07/R08 were stale harness
        counts, 5 stat cards + 7 small EVM tiles - harness updated).
        T2 battery, CI poll, ship per standing order.

--------------------------------------------------------------------
STANDING RULES RECORDED THIS SESSION (owner, verbatim intent)
--------------------------------------------------------------------
R1. NO EMDASHES anywhere on a served page, ever. Rule goes into the
    continuation directive + reflection + creator skill. The "finish
    line" section on index.html: remove the giant dash + drop the
    "the finish line" micro-label; keep plain flow rough -> final.
R2. PLAIN LANGUAGE ONLY in product copy. No jargon ("hashed", "stored
    locally" walls of text). Short sentences. Explanations live in a
    short note line, not paragraphs. Skill: plain-language-copy.
    Admin setup screen: fields are New admin password + Confirm. One
    short note is allowed (e.g. password stays on this device).
R3. CONSISTENCY BY REFERENCE: when building any UI element, first find
    a similar element ALREADY in the app (tooltips, chips, modals) and
    match its structure/behavior. Never invent in isolation. Goes into
    a skill + directive.
R4. NO SLACKING: never give up on a task, never ship a partial fix
    silently, never claim done without verification. Skill required
    (owner: "Don't slack off" / anti-lazy skill). Owner reports the AI
    previously gave up mid-task (e.g. tooltip backgrounds).
R5. HUMAN AUDIT SKILL: distill this audit's review method (how the
    owner walks every page, sidebar, tooltip, toggle, theme) into a
    reusable skill for future audits.
R6. SIGN-IN IS APP-ONLY: sign-in entry points live on app.html (and
    admin). Signed-in state may SHOW on other pages but the ACT of
    signing in starts from the app section. Project sidebar shows
    project navigation ONLY (no "Not signed in" - remove; the backup
    section is the right home for a "not backed up -> sign in" hint).
R7. DARK THEME = BLACK AND WHITE (already the contract) + the starry
    background + advanced UI must also cover the ADMIN page (admin
    gets the same app-level premium treatment).
R8. Domain is LIVE: https://mymanagerworkspace.com (bought, paid).
    Update directive + reflection + planning + skills with the domain
    and the new capabilities a paid domain unlocks (proper email via
    a subdomain, forgot-password email flows, professional URL).
    Owner wants a FREE email servicing path researched (Resend free
    tier) for password/admin-code recovery emails.
R9. Offline admin codes are the USER's responsibility, not ours.
    Signed-in users on a new device get an "Enter my admin code"
    path so a cloud-linked admin can restore access after sign-in.
R10. Buttons on marketing: Get Started returns to GOLD; secondary
     buttons stay glass but turn gold on hover; sizes follow standard
     landing-page button dimensions (research-backed), roughly 1.2x
     smaller than current oversized state.
R11. Code must read cleanly for a mid-to-senior engineer. No clever
     one-liners that hide intent.
R12. Push every increment; CI must be green (and RUNNING - owner says
     CI was not running; investigate) before deploy. Deploy to BOTH
     domains (workers.dev + custom domain).
R13. Wrangler must be updated (owner noticed staleness).

--------------------------------------------------------------------
EXECUTION PLAN (one by one, in this order)
--------------------------------------------------------------------

PHASE 0 - PLAN + RULES (this file)
P0.1 [x] Write this tracker.
P0.2 [x] Add standing rules to CONTINUATION-DIRECTIVE.md (em-dash ban,
       plain language, consistency-by-reference, domain live, sign-in
       app-only, sidebar purity, admin premium parity).
       DONE (verified 2026-09-11): all rules present in the directive's
       OWNER STANDING RULES section (rules 1-6 at lines ~214-252; sidebar
       purity folded into rule 4). Only the checkbox had lagged.
P0.3 [x] Check CI is running on GitHub (owner: it was not visible);
       check wrangler latest; update wrangler if behind.
       DONE 2026-09-09: CI run 264 IS green on main (owner's worry
       cleared; Nightly vs CI are separate workflows). Wrangler bumped
       4.129.1 -> 4.130.0 (package.json + lock). FOUND + FIXED the
       nightly red streak (6 runs): qa-cloud-phase2.cjs never applied
       WRANGLER_DEV_URL to its API BASE (fell back to chrome-launcher's
       :8765 where nothing listens in CI) and nightly.yml never passed
       ADMIN_CODE=QA-ADMIN-CI (phase2 P4.1a 403 + cascade). Fixed both
       (BASE fallthrough line + external-mode boot canary via
       /api/cloud/sections + nightly env parity with ci.yml).
       VERIFIED: 85/85 external-mode local run (nightly contract).

PHASE 1 - SKILLS (owner-ordered creations)
P1.1 [x] Skill: human-audit (the walk-the-whole-site review method
       distilled from this audit).
P1.2 [x] Skill: no-slacking (anti-lazy: finish what you start, verify,
       never abandon mid-fix; include the tooltip/consistency example).
P1.3 [x] Skill: plain-language-copy (banned jargon list, note-line
       pattern, examples from this audit).
P1.4 [x] Register all three in skills-lock.json via the hash tool;
       npm run verify green (20/20 skills, ALL CHECKS PASSED).

PHASE 2 - ADMIN PANEL (owner's admin audit)
P2.1 [x] Admin setup/login copy: strip the jargon wall. Fields: New
       admin password + Confirm. One short note line max.
       DONE 2026-09-09: setup heading kept, paragraph now "Choose an
       admin password. It stays on this device." (was a 4-line
       hashing/storage wall). Lost-code + reset-confirm copy
       shortened. CSP hashes regenerated (24, worker+serve), sw
       v267, bundles rebuilt. verify ALL PASS; verify-controls-admin
       11/11 (with serve.cjs up); qa-admin-recovery 24/24.
P2.2 [x] Premium starry glass + story background + advanced UI on
       admin.html (parity with app section, owner explicit request).
       DONE 2026-09-09: js/mmgr-glass.js activate() admin exclusion
       removed (project.html stays excluded). Admin already carried
       the glass-premium z-index layering (gate+header+main) from the
       dock wave. Rebuilt bundles; live-probed admin: glassClass true
       + canvas mounted + effective premium. Harnesses aligned to the
       drawer-select theme contract: verify-glass-preview-cdp (G3
       dockHasTheme) OK, qa-view-mode V1/V2 retargeted + V3 now
       mirrors Perf.apply() (localStorage write alone left data-perf=on
       which blocked the tilt) - ALL VIEW-MODE GATES PASSED.
P2.3 [x] Signed-in admin on a NEW DEVICE: after Google sign-in, offer
       "Enter my admin code" (cloud-linked restore) instead of only
       "create a new admin code".
       DONE 2026-09-09: admin.html setup screen gains a hidden-until-
       signed-in restore block (divider "or restore access" + password
       field + Restore access button + one note line). Visibility =
       event detail OR GA.isSignedIn(); deterministic SHA-256 means the
       same admin code re-creates access on the fresh device, then the
       one-time recovery modal issues as usual. Unsigned visitors see
       only the create form. Live browser-verified end to end (setup ->
       wipe -> signed-in event -> restore field appears -> same code ->
       recovery modal -> panel unlocked). CSP hashes regen (worker+
       serve, 24), sw v270, bundles rebuilt. verify ALL PASS;
       verify-controls-admin 11/11; qa-admin-recovery 24/24;
       qa-view-mode ALL. GOTCHA hit + fixed: .rc-divider[hidden] needed
       a display:none guard (verify:hidden gate caught it); a stale
       serve.cjs process served old CSP hashes and blocked every inline
       script (killed by exact PID, restarted).

PHASE 3 - PROJECT PAGE (owner's project audit)
P3.1 [x] DONE 2026-09-09: signed-out visitors get no account row (#sb-user
       hidden+empty, sidebar navigation-only); signed-in keeps name + Sign
       out. Live probe verified; no dead references remain.
P3.2 [x] DONE 2026-09-09: #bk-signin row in the backup popover, shown while
       not cloud-linked + signed out, links to app.html (sign-in lives only
       there per the 2026-09-07 rule). Re-render wired to mmgr:user-changed
       + mmgr:google-signed-out. CSP hashes regenerated (24) + meta CSP 4th
       hash restored; .bk-opt[hidden] + .db-user[hidden] guards added
       (verify-hidden green). qa-view-mode ALL after polling fix for V3.
P3.3 [x] DONE 2026-09-09: .bk-pop switched to solid var(--card) fill (the
       .mlc-card methodology pattern); the dark-mode override that forced
       7%-white glass back on was removed. Readable in both themes.
P3.4 [x] DONE 2026-09-09: dark mode remaps --gold to white -> white-on-white
       FAB. body.dark-mode .calc-fab now pins the light-theme brand orange
       (#D96B27), mirroring the AI FAB's fixed-color precedent the owner
       endorsed. Live probe: bg rgb(217,107,39) + white glyph in dark.
P3.5 [x] DONE 2026-09-09: .sr rows reworked (align-items:center; gap:14px;
       min-height:34px; label flex:1; control margin-left:auto) - labels
       breathe, toggles/inputs/buttons pin to the RIGHT edge (same pattern
       as the dock's .pal-switch). Live probe: Your Name gap 14px, input
       flush right; all Controls rows measured. Applies to the cloud-drawer
       .sr rows identically (consistency). qa-cloud-phase1 29/29 after
       freeing port 8765 (serve.cjs was squatting on the harness port -
       known gotcha, note for future sessions).
       Aug 31 git version for the workspace structure that was right.
P3.6 [x] DONE 2026-09-09: root cause = commit 8e4b8df gutted the .sr row
       rule to bare display:flex. Aug 31 structure restored (bordered rows,
       11px padding, 7px rhythm, space-between) merged with right-edge
       control alignment. Probes + gates green.

PHASE 4 - APP LAUNCHER (owner's app audit)
P4.1 [x] DONE 2026-09-10: root cause was TWO stacked bugs. (1)
       css/mmgr.css had body.db-page .db-signin{display:none} (the
       2026-09-07 decision that hid the footer button), leaving the
       51px dark footer strip EMPTY (the owner's black box). (2)
       restoreSession()'s signed-out path never fired mmgr:user-changed
       (fires only when a session is found), so renderRailUser() never
       ran and the static footer (button only) was what rendered.
       FIX: removed the display:none (button visible again; hover/focus
       rules intact), deleted the dead .db-user-name.is-out rule, and
       renderRailUser(null) now emits the SAME plain Sign in button as
       the static markup (no "Not signed in" status text - owner rule).
       VERIFIED live via CDP: button display:flex 84x28, no empty box,
       click opens #siom (email field visible + "or continue with
       Google" divider). GOTCHA re-hit: serve.cjs bakes CSP hashes at
       STARTUP - it was started before the edits, so Chrome blocked all
       inline scripts silently (siomBound:false); restarting serve.cjs
       after edits fixed it. sw v272, bundles + CSP hashes (24) regen,
       npm run verify ALL PASS.
P4.2 [x] DONE 2026-09-10: root cause = the --db-* dark token block
       still carried the warm-brown family (--db-surface:#242019,
       raised #2e2922, border #3a342c) from the pre-B&W palette; the
       2026-09-07 pass whitened the accent but never the surfaces.
       FIX (semantic tier, so every card inherits): --db-surface
       #141416, raised #1d1d20, border #2b2b30 (neutral jet-black
       family). All launcher cards (demo + user-created) share .pcard,
       so newly created projects get the same treatment automatically.
       ALSO: stray fluorescent cyan rgba(80,232,244) on .cd-plan.at-limit
       swapped to white (universal-ui-architect hard gate: never
       fluorescent cyan). Demo Projects heading 13.12px -> 26.24px
       (exactly 2x, owner-approved). White hover border kept (owner).
       VERIFIED live via CDP: cardBg rgb(20,20,22), border rgb(43,43,48),
       title rgb(245,245,245), heading 26.24px. Light mode untouched
       (scoped to body.dark-mode.db-page). sw v273.
P4.3 [x] DONE 2026-09-10: renderProjectResults() in app.html had a
       per-word AND filter but NO ordering. Added prefix-first ranking
       on the first typed word: tier 0 = title starts with it, tier 1
       = a title word starts with it (non-alphanumeric boundary),
       tier 2 = inside a title word, tier 3 = description-only;
       alphabetical (localeCompare) inside each tier. Filter semantics
       unchanged (all words must match somewhere). VERIFIED live via
       CDP: query "pro" -> [Blank Template Project, QA Demo Project]
       (word-start tier) above Riverside (description-only tier);
       query "qa" -> QA Demo Project (title-prefix, tier 0);
       query "e" -> alphabetical within tier. Live typing re-sorts
       per keystroke via the existing input listener. CSP hashes
       regen (inline script), sw v274, bundles rebuilt.

PHASE 5 - MARKETING / SIGN-IN (owner's marketing audit)
P5.1 [x] DONE 2026-09-10: root cause = css/marketing.css line 283 had
       a SELECTOR LEAK: "body.dark-mode .signin-btn,.btn-gold{...}" -
       the unscoped .btn-gold applied the dark sign-in GLASS skin to
       every gold button in EVERY theme (later in file = beat the real
       gold rule). Get Started had no gold anywhere. FIX: unscoped
       selectors removed (glass stays on dark .signin-btn only);
       .btn-gold resized 60px->49px (padding 20x40->15x28, font
       1.12->1.05rem, radius 16->14px) per landing-page research
       (44-48px tap target, 51.76px high-converting average, radius
       ~30% of height, ~1.2x down as owner specified); .btn-ghost-light
       hover now turns GOLD (tint bg + gold border + gold text). VERIFIED
       live via CDP: hero bg rgb(217,107,39), h=49, radius 14px; ghost
       hover rgba(217,107,39,.16)/gold border/gold text. Light mode only
       scope - marketing pages are theme-decoupled.
P5.2 [x] DONE 2026-09-10: em/en dash scan across all 15 served pages
       + js/*.js = ZERO hits (the v246 sweep held; nothing regressed).
       The owner-flagged "The finish line" eyebrow (and its decorative
       dash) removed from the interior photo band on index.html; the
       section flows straight into "From rough-in to the final
       walkthrough."
P5.3 [x] DONE 2026-09-10: root cause = the "LOCK-IN (OWNER 2026-08-17)"
       logic in js/marketing.js added .done to every section scrolled past
       and NEVER removed it, so labels/stays lit on the way back up. FIX:
       one-way lock-in replaced by a symmetric recompute in setSpy():
       active index drives the trail (above = done, active = lit, below =
       un-lit); the stale add-done branch removed from the observer.
       Smooth tracking unchanged (CSS .2s transitions + html smooth
       scroll). VERIFIED live via CDP: at #faq -> builtfor..guide done +
       faq active; scroll back to #builtfor -> builtfor active, all
       others none (fully un-lit).
P5.4 [x] DONE 2026-09-10: marketing signin-sheet reordered to the
       owner's layout: email form FIRST (Email + Password + Sign in),
       divider, Continue with Google, then the links (Forgot
       password? / Create account) moved BELOW the Google button and
       free-flowing. The email form's solid card box REMOVED (owner:
       "let them free flow", no borders) - transparent bg, no border,
       no padding; inputs keep their field styling. Markup: index.html
       #signin-sheet order swapped; js/marketing.js moves .email-auth-alt
       after #google-signin-button at mount (reset-panel alt stays put);
       css/marketing.css de-boxes .signin-sheet .email-auth. VERIFIED
       live via CDP: sheetOrder [marketing-email-auth, divider, google,
       alt, note, user], formBox bg transparent/border none/padding 0,
       forgot link visible. sw v276, bundles rebuilt.

PHASE 6 - LOCAL AI ASSISTANT (owner: rate limit too extreme)
P6.1 [x] DONE 2026-09-10: root cause = localLookup()'s intent list in
       js/mmgr-ai.js had NO forecast intent ("forecast" only matched via
       /weather|delay/, which needs weather/delay words), so "what is
       the project forecast?" fell to the honest-refusal branch. AUDIT
       of the rest of the path: the local tier has NO other limiter
       (_aiBusy serialization only); the /api/ai relay (src/ai-proxy.js)
       adds none either (256KB body cap + upstream 429/503 passthrough
       only) - the "extreme rate limit" the owner felt WAS the intent
       matcher refusing. FIX: new forecast intent (forecast|projected
       finish|finish date|completion date|eta|when-will-we-finish)
       reuses the grounded LOCAL_BUILDERS.forecast (projected finish vs
       charter target + CPI trend + weather delays, trace preserved);
       EVM matcher widened with "burn rate". Chit-chat STILL refused
       (owner wants that). VERIFIED live via CDP on demo-filled:
       forecast question -> FORECAST block (2026-06-30 on target, CPI
       0.87, 4 wx delays); "is our EVM good?" + "how is our CPI
       looking?" -> EVM numbers; "hello" + "tell me a joke" -> refused.
       sw v277 (bundle.js is a SHELL asset), bundles rebuilt.

PHASE 7 - DOMAIN + EMAIL (owner: subdomain email + free email service)
P7.1 [x] DONE 2026-09-10: researched + audited. Resend free tier =
       3,000 emails/mo, 100/day, 1 verified sending domain - ample for
       recovery email. KEY FINDING: the integration is ALREADY BUILT and
       dormant in the codebase (sendAuthEmail() in src/lib/http.js calls
       Resend's API; RESEND_API_KEY/RESEND_FROM_EMAIL documented in
       .dev.vars.example; EMAIL_RECOVERY_ENABLED:false in wrangler.jsonc
       from the v233 Tier A work; subdomain approach already hinted in
       wrangler.jsonc comments). No new code needed - only owner DNS +
       secret setup. Recorded in OWNER-REVIEW.md (P7.2).
P7.2 [x] DONE 2026-09-10: OWNER-REVIEW.md Domains & Email section now
       carries the full step-by-step walkthrough: (A) outbound via a
       mail.mymanagerworkspace.com SUBDOMAIN on Resend's free tier
       (add domain, add Resend's DKIM/SPF/DMARC records in Cloudflare
       DNS as DNS-only/grey-cloud, verify, wrangler secret put
       RESEND_API_KEY + RESEND_FROM_EMAIL, flip EMAIL_RECOVERY_ENABLED,
       deploy, test); (B) inbound contact address via Cloudflare Email
       Routing (free, forwards to Gmail, auto MX); (C) OAuth origins for
       both apex + www. Note: offline admin codes remain the guaranteed
       recovery path (owner rule R9) - email is the convenience layer. No bloat features - improve reach/access.
P7.3 [x] DONE 2026-09-10: CONTINUATION-DIRECTIVE.md owner standing rule
       6 (DOMAIN IS LIVE) now carries the domain-capabilities summary:
       email stack built-but-dormant (sendAuthEmail +
       EMAIL_RECOVERY_ENABLED), activation is owner-side via the
       OWNER-REVIEW.md walkthrough, free-tier numbers, offline codes
       stay the guaranteed path. Planning tracker updated per phase
       throughout this session. Skills audited: the three 2026-09-09
       skills (human-audit / no-slacking / plain-language-copy) are
       method skills with no stale domain references; no skill edits
       needed. Historical session logs in the directive keep their
       workers.dev URLs as history (rule 6 already mandates BOTH
       origins on every future deploy). reflection.txt written at
       session end (P9.3).

PHASE 8 - VERIFICATION + SHIP
P8.1 [x] node build.js (bundles), npm run verify, emoji scan, em-dash
       scan, targeted qa-*.cjs suites. DONE 2026-09-10/11: verify ALL
       CHECKS PASSED; emoji + em-dash scans CLEAN; full local CI
       replication green (T1 7/7 suites; T2 phase1 29/29, phase2 85/85,
       email-auth 69/69, import 35/35, reviews 25/25, rank9 31/31, t9
       27/27, presence 11/11, prefs 15/15, ai-badge 5/5, market-features
       61/61, offline-copies 23/23, controls-admin 11/11, dashboard-spec
       85/85, marketing 20/20, AI23_GATE PASS; wrangler --dry-run exit 0).
P8.2 [x] Commit (Conventional Commits, NO AI footer), push, POLL the
       Actions API until green. DONE: 7068502 feat(ui) wave + 5e6fdc2
       docs(session) bookkeeping pushed to main; CI run 34563359120
       completed SUCCESS on 5e6fdc2 (owner was notified the commit was
       coming per their ship order 2026-09-10).
P8.3 [x] Deploy from clean staging (wrangler.jsonc recipe) to BOTH
       domains; smoke-test live. DONE 2026-09-11: version 57b6463d
       deployed from /tmp/mmgr-deploy (debris-free staging, 78 top-level
       entries, no .dev.vars/scratch/screenshots). Live smoke: home 200,
       sw mmgr-shell-v277 served, /api/health ok, rail sign-in CSS fix
       live, B&W #141416 tokens live, gold-leak selector gone, visible
       "finish line" text 0, forecast intent in bundle.js, admin/app 307
       (production .html rewrite, expected). Both domains serve the same
       Worker (workers.dev + mymanagerworkspace.com).

PHASE 9 - BOOKKEEPING (after EVERY phase, not just the end)
P9.1 [x] Update this file per phase. DONE (updated after P4.1, P4.2,
       P4.3, P5.x, P6.1, P7.x, P8.1; ship record added 2026-09-11).
P9.2 [x] Update CONTINUATION-DIRECTIVE.md STATUS LOG per phase. DONE:
       2026-09-10 session entry written (was stuck at 2026-09-06); ship
       record appended to the same entry via docs commit 5e6fdc2.
P9.3 [x] Write reflection.txt at session end (post-mortem). DONE:
       2026-09-10 session post-mortem appended (context, accomplishments,
       6 lessons, outstanding items).

--------------------------------------------------------------------
NOTES
--------------------------------------------------------------------
- Existing sessions' lessons apply: NEVER taskkill node by image name;
  rebuild bundles after js/ edits; RAF-wrapped renderers need a flush
  before DOM checks; D1 WAL lag needs retry; staging-copy deploy only.
- The skeptical-code-audit skill is MANDATORY for verification.
- Web research (Serper + read_url) and Playwright/BeautifulSoup (via
  web-research/*.py patterns) are available for any doubt.

--------------------------------------------------------------------
SESSION 2026-09-19 (Task 13 + 14: SYNC BOND + PLAN CLOSEOUT)
--------------------------------------------------------------------
Owner order: read the empty "last sessionm.txt", clean up last
session, complete what is left. Verified the 14-task plan doc: Tasks
1-11 committed; Task 13 (sync bond) not started; Task 14 (docs
closeout) open. Cleanup: deleted the empty notes file + all session
txt debris (Freebuff*/Windows PowerShell*/crashes), emptied tmp/ (197
files), removed the stale $HOME/mmgr-deploy staging copy.

TASK 13 SHIPPED - sync bond (file trips keep the cloud twin link):
- js/mmgr-state.js: exportState embeds cloudBond {cloudProjectId,
  linkedAt} when the cloud module reports a link; importState stores
  mmgr_cloud_bond_<localId> + raises State.isBondPending. Bond is a
  POINTER, never a credential (no owner code rides the file).
- js/mmgr-cloud.js: bond helpers + adoptBondId (re-points
  mmgr_cloud_id_<pid> at the twin); bonded createProject probes with
  held code first, session fallback, sign-in challenge otherwise (the
  Task 13 resolution order) - never a blind create; cloudResync (load
  + State.mergeExternal, newest-wins per field, honest status); one-
  time offer card + Later dismiss; Re-sync row with lastSyncedAt in
  linked branches; successful re-sync clears the offer.
- js/mmgr-app.js: cloudResync/cloudBondLater ACTION_MAP entries;
  cloudResync deliberately OUTSIDE READONLY_SAFE_ACTIONS (a merge can
  overwrite local fields - same honesty contract as cloudLoad/import);
  cloudBondLater is dismiss-only, safe.
- tools/qa-sync-bond.cjs (new, CI T2 port 8810): 20/20 - D1 bond
  embed/no-credential, D2 fresh-device detection + offer, D3 re-sync
  arrival + newest-wins + offer cleared, D4 B pushes back + cloud
  carries it, D5 sign-in challenge + bond kept + no duplicate row,
  D6 live bond shape.
- HARNESS LESSONS: (1) ev() on a closed CDP socket hangs the run -
  capture the last in-page value BEFORE switching device contexts
  (D6 was captured on device B2, not after the ghost context); (2)
  drawer-button queries must scope to #cloud-section - Share &
  Access renders its own cloudCreate button earlier in the DOM (the
  app was correct; the harness read the wrong node); (3) CI's general
  rate bucket is shared by code-less /meta render probes - dense
  anonymous phases can 429 a later direct probe.
- TOOLS FIX (pre-existing, not this wave): qa-date-wiring W-gates
  computed expectedDate in UTC (toISOString) while the app's todayStr
  is local - off-by-one west of Greenwich, silently 28/32 on the
  owner's machine while green on UTC CI runners. Fixed to local
  convention: 32/32.

TASK 14 DONE - docs closeout:
- docs/DEVELOPER-GUIDE.md: js/app/* row gained baseline-guard +
  entitlements; mmgr-tasks row gained Task 1/3 notes + updTaskField
  split-candidate watch; mmgr-cloud row gained the sync-bond summary
  + current line count; mmgr-watch row added.
- Plan doc Task 13/14 checkboxes marked with evidence; reflection.txt
  session record written.

GATES: qa-sync-bond 20/20; verify-date-wiring 13/13; qa-date-wiring
32/32; npm run verify ALL CHECKS PASSED; bundles rebuilt; emoji scan
clean on touched files. Commit + CI poll + deploy per protocol.

SHIP RECORD (same session): commits e70ef10 (feat) + f72f6b0 (docs)
pushed with the pending Task 10 commit 011bda5; CI run 35479735797
polled to SUCCESS (new T2 sync-bond step included). Deployed from
verified clean staging ($HOME/mmgr-deploy, 84 entries, .dev.vars
stripped, new-string greps green) -> Version 210bcc6e-c0a6-48de-97af-
577a1bc0b766. Live smoke BOTH domains: /api/health ok, served
dist/bundle.js + js/mmgr-state.js carry the bond code (2 + 3 hits).
Also removed a stray empty "-p" directory that a prior session's
tar invocation had dropped at the repo root (now in staging excludes
by never creating it; nothing tracked was inside).

GUIDE WAVE (same session, owner follow-up): field guide A-15 gained
two plain-language flow cards - what the cloud bond is (exported
files carry a quiet link to their cloud copy, no codes in the file)
and how Re-sync works (newest-wins per field, sign-in when no code
is held, bond kept on failure). Banned-vocab + emoji + dash scans
clean; sw.js v304 -> v305 for the guide edit; directive STATUS LOG
Session 9 entry written. Docs-only wave: no inline scripts touched,
CSP hashes unchanged, npm run verify green. SHIP: commit c5f5f05
pushed, CI run green (polled), deployed from verified clean staging
-> Version cfaa6a61-e71a-4627-8b52-e45bc447fb15; live smoke BOTH
domains: guide serves the new cards, sw.js at the edge is v305.

2026-09-20 - Session 10 (TWO-WAY SYNC + SIGN-IN WIRING + HAVE-A-CODE +
DARK BUTTON LANGUAGE). Owner-reported wiring gaps, all root-caused then
fixed: (1) signed-out Backup-to-cloud click now routes to sign-in and
resumes the create after (was a silent dead end); (2) header chip
honesty - green "Synced to cloud" for owner/editor/session links (was
"Not backed up" on synced session-owner devices), "Viewing cloud copy"
for read-only; (3) sync is now TWO-WAY - 60s pull watcher for
owner/editor links, per-field newest-wins, echo-suppressed, mid-edit
safe, view-only off; (4) AI cloud engine consults Entitlements;
(5) project.html sign-in fallback opens the drawer's cloud section;
(6) app.html "Have a code?" wired end-to-end (lookup -> snapshot ->
session seeds -> project boot; 16-char AND 8-char client codes) with a
plain-language tooltip; (7) dark-theme button language (project pages
only): pills -> rounded rectangles, background-mimicking rest state,
gold on hover only, 1.2x button icons, tabs mimic the background;
light theme untouched. Gates: npm run verify, verify-date-wiring 13/13,
qa-date-wiring 32/32, qa-ai-relay PASS, qa-sync-bond 20/20,
qa-client-codes ALL, qa-dashboard-spec 76/76. SW v306.
2026-09-20 CI REPAIR LOOP (run for the two-way-sync wave) - 3 cycles to green on 6f8297c:
  1. add8d48 delegate repair pushed -> next step surfaced: T1 dynamic-labels died ECONNREFUSED 127.0.0.1:9245
     (Chrome boot flake: readiness poll passed, /json fetch crashed bare). Fixed harness: bounded /json retry
     with self-describing error, clean throw on no page targets, one full boot retry on port 9246 + fresh
     profile (525550c). Step passed next run.
  2. Next latent failure surfaced (loop law): T2 phase1 C4c/C4d/C4h - my session guard inside createProject
     broke the anonymous fresh-create contract (owner code IS the credential). Guard removed; backup pill
     chains checkMe (newly exported) AFTER drawer render: signed-in -> create directly, signed-out -> rendered
     sign-in host (signIn needs #cloud-gis-host). Local: phase1 29/29, sync-bond 20/20, client-codes green (6f8297c).
  3. CI run SUCCESS on 6f8297c. Deployed via verified staging -> Version 31c4222a-ae82-495f-937d-381d3e011d4b.
     Live smoke both domains (workers.dev + mymanagerworkspace.com): health 200, bundle carries c.checkMe(!1).
  LAW reinforced: a browser harness that polls a DevTools port must tolerate the fetch dying right after the
  poll, and every boot path needs one retry - runners flake, the run is the truth.
2026-09-20 FOLLOW-UP (Session 10b closeout) - field guide A-15 backup card states the anonymous-create
  contract in plain language (first click opens sign-in, one more click creates the cloud copy + shows the
  16-character owner code once; "picks up where it left off" overstatement corrected - the signed-out path
  queues nothing), banned-vocab + emoji + dash gates clean on the edited line, SW v306 -> v307 for the
  served-guide edit. Session scratch cleaned: tmp/ emptied, $HOME/mmgr-deploy staging copy removed.
