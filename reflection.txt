Session: 2026-08-31 (UI Fixes Continuation - Round 5)
Deploy: 172530b2-85d5-4a3e-9516-074da4d604bc (v221)

Completed this session:
1. Admin sidebar completely rewritten to use db-side class system
   matching app.html layout (db-side-head, db-nav, db-link, db-g,
   db-sub, db-foot). Body gets admin-page + db-page classes.
2. Admin rail footer: renderRailUser() shows name + sign out when
   signed in, Sign in button when signed out. mmgr:user-changed
   listener hides email-auth-block + google-signin-button when
   Google-signed-in. openSignIn action wired.
3. Search moved from inline grid input to existing search modal.
   Modal now includes cloud + demo projects. Search button added
   to section header beside New Project.
4. Controls accordion caret icons: 14px gold arrows, absolutely
   positioned on right side of section headers.
5. Field guide sign-in: restyled as bordered chip with fixed-position
   glass panel matching marketing page pattern.
6. Admin FOUC script sets side-open on desktop for db-side CSS.
7. tglAdminNav syncs side-open class; click handler recognizes
   both db-ctl-row and rail-ctl-row.

Tests: verify ALL PASSED, qa-marketing 20/20, qa-dashboard-spec 77/77

Lessons:
- Admin sidebar had to be converted from rail-group/rail-link classes
  to db-side/db-link/db-g classes to match app.html visual layout
- The mmgr:user-changed event is the canonical way to sync auth state
  across all surfaces; mountEmailAuth inserts email-auth-block as
  sibling after google-signin-button, so hiding both is required
- Search modal already had real-time filtering; the inline grid input
  was redundant - just needed the modal to include all project types
